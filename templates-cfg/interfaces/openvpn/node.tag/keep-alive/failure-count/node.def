type: u32
default: 6
syntax:expression: $VAR(@) >= 1; "failure-count must be greater than 0"
help: Maximum number of keepalive packet failures
val_help: u32; Maximum number of keepalive packet failures
